# Phase 0 — Data Pipeline Dependencies

# PDF Processing
pymupdf==1.25.3

# LangChain — AI Orchestration
langchain==0.3.25
langchain-community==0.3.24

# Embeddings (local, uses PyTorch — same PyTorch Whisper needs in Phase 1)
sentence-transformers==3.3.1

# Vector Database
chromadb==0.6.3

# Environment Variables
python-dotenv==1.1.0
